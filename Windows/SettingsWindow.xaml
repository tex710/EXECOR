<Window x:Class="HackHelper.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" 
        Height="490" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize">

    <Window.Resources>
        <!--========  GLOBAL FONT  ========-->
        <Style TargetType="TextElement">
            <Setter Property="FontFamily" Value="pack://application:,,,/Resources/EurostileBold.ttf#Eurostile Bold"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="pack://application:,,,/Resources/EurostileBold.ttf#Eurostile Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>
        <Style TargetType="Control">
            <Setter Property="FontFamily" Value="pack://application:,,,/Resources/EurostileBold.ttf#Eurostile Bold"/>
        </Style>
        <!--================================-->

        <!-- Window Control Buttons -->
        <Style x:Key="WindowButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource BorderBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource WindowButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SecondaryBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Grid Width="24" Height="24">
                                <!-- Outer Box -->
                                <Border x:Name="OuterBorder"
                                Width="24" 
                                Height="24"
                                Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource SurfaceBrush}"
                                BorderThickness="2"
                                CornerRadius="6"/>

                                <!-- Inner Gradient Box (shown when checked) -->
                                <Border x:Name="InnerBorder"
        Width="16" 
        Height="16"
        CornerRadius="4"
        Opacity="0"
        HorizontalAlignment="Center"
        VerticalAlignment="Center">
                                    <Border.Background>
                                        <LinearGradientBrush x:Name="CheckboxGradient"
                             StartPoint="0,0"
                             EndPoint="1,0"
                             SpreadMethod="Repeat">
                                            <GradientStop Color="{DynamicResource SecondaryColor}" Offset="0.3"/>
                                            <GradientStop Color="{DynamicResource PrimaryColor}" Offset="1.0"/>
                                            <GradientStop Color="{DynamicResource SecondaryColor}" Offset="0.3"/>

                                            <LinearGradientBrush.RelativeTransform>
                                                <TransformGroup>
                                                    <TranslateTransform x:Name="CheckboxGradientTranslate" X="0"/>
                                                    <RotateTransform Angle="40" CenterX="0.5" CenterY="0.5"/>
                                                </TransformGroup>
                                            </LinearGradientBrush.RelativeTransform>
                                        </LinearGradientBrush>
                                    </Border.Background>

                                    <Border.Effect>
                                        <DropShadowEffect Color="{DynamicResource PrimaryColor}"
                          BlurRadius="10"
                          ShadowDepth="0"
                          Opacity="0.8"/>
                                    </Border.Effect>

                                    <Border.Triggers>
                                        <EventTrigger RoutedEvent="Loaded">
                                            <BeginStoryboard>
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation
                        Storyboard.TargetName="CheckboxGradientTranslate"
                        Storyboard.TargetProperty="X"
                        From="0"
                        To="1"
                        Duration="0:0:1.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Border.Triggers>
                                </Border>
                            </Grid>

                            <ContentPresenter Margin="8,0,0,0" 
                                      VerticalAlignment="Center"
                                      Content="{TemplateBinding Content}"/>
                        </StackPanel>

                        <ControlTemplate.Triggers>
                            <!-- When Checked -->
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="InnerBorder" Property="Opacity" Value="1"/>
                            </Trigger>

                            <!-- Hover effect on unchecked -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="InnerBorder" Property="Opacity" Value="0.5"/>
                            </MultiTrigger>

                            <!-- Hover effect on checked -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="InnerBorder" Property="Opacity" Value="0.75"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                        Background="{TemplateBinding Background}"
                        CornerRadius="6"
                        Padding="{TemplateBinding Padding}">
                            <Grid>
                                <TextBlock x:Name="NormalText"
                                   Text="{TemplateBinding Content}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Foreground="{TemplateBinding Foreground}"/>
                                <TextBlock x:Name="GlowText"
                                   Text="{TemplateBinding Content}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Visibility="Collapsed"
                                   Foreground="{DynamicResource TextBrush}">
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="White" BlurRadius="12" ShadowDepth="0" Opacity="1"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="{DynamicResource PrimaryColor}" Offset="0"/>
                                            <GradientStop Color="{DynamicResource SecondaryColor}" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="GlowText" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="NormalText" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton 
                                Name="ToggleButton" 
                                Grid.Column="2"
                                Focusable="False"
                                IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                ClickMode="Press">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                                        
                                        
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border 
                                                        Background="{TemplateBinding Background}" 
                                                        
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="6">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition />
                                                                <ColumnDefinition Width="20" />
                                                            </Grid.ColumnDefinitions>
                                                            <Path 
                                                                Grid.Column="1"
                                                                HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"
                                                                Data="M 0 0 L 4 4 L 8 0 Z"
                                                                Fill="{DynamicResource SubTextBrush}" />
                                                        </Grid>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background">
                                                    <Setter.Value>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="{DynamicResource PrimaryColor}" Offset="0"/>
                                                            <GradientStop Color="{DynamicResource SecondaryColor}" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="BorderBrush">
                                                    <Setter.Value>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="{DynamicResource PrimaryColor}" Offset="0"/>
                                                            <GradientStop Color="{DynamicResource SecondaryColor}" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                            <ContentPresenter 
                                Name="ContentSite"
                                IsHitTestVisible="False" 
                                Content="{TemplateBinding SelectionBoxItem}"
                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                Margin="10,3,23,3"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                            <Popup 
                                Name="Popup"
                                Placement="Bottom"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                AllowsTransparency="True" 
                                Focusable="False"
                                PopupAnimation="Slide">
                                <Grid 
                                    Name="DropDown"
                                    SnapsToDevicePixels="True"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border 
                                        x:Name="DropDownBorder"
                                        Background="{DynamicResource SurfaceBrush}"
                                        BorderThickness="2"
                                        BorderBrush="{DynamicResource PrimaryBrush}"
                                        CornerRadius="6"
                                        Margin="0,2,0,0">
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border 
    Name="Border"
    Background="{TemplateBinding Background}"
    BorderThickness="0"
    CornerRadius="4"
    Padding="{TemplateBinding Padding}"
    Margin="2">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="{DynamicResource PrimaryColor}" Offset="0"/>
                                            <GradientStop Color="{DynamicResource SecondaryColor}" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="{DynamicResource PrimaryColor}" Offset="0"/>
                                            <GradientStop Color="{DynamicResource SecondaryColor}" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- REMOVED colored border, kept clean background -->
    <Border Background="{DynamicResource BackgroundBrush}" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar with Animated Gradient Text -->
            <Border Grid.Row="0" Background="#0F0F12" CornerRadius="12,12,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="40">
                    <!-- Stylized Title Text with Gradient Animation -->
                    <TextBlock Text="SETTINGS"
                               FontFamily="pack://application:,,,/Resources/EurostileBold.ttf#Eurostile Bold"
                               FontSize="18"
                               FontWeight="Bold"
                               VerticalAlignment="Center" 
                               Margin="15,0,0,0">
                        <TextBlock.Foreground>
                            <LinearGradientBrush x:Name="TitleGradient"
                                                 StartPoint="0,0"
                                                 EndPoint="1,0"
                                                 SpreadMethod="Repeat">
                                <GradientStop Color="{DynamicResource SecondaryColor}" Offset="0.3"/>
                                <GradientStop Color="{DynamicResource PrimaryColor}" Offset="1.0"/>
                                <GradientStop Color="{DynamicResource SecondaryColor}" Offset="0.3"/>

                                <LinearGradientBrush.RelativeTransform>
                                    <TransformGroup>
                                        <TranslateTransform x:Name="GradientTranslate" X="0"/>
                                        <RotateTransform Angle="-40" CenterX="0.5" CenterY="0.5"/>
                                    </TransformGroup>
                                </LinearGradientBrush.RelativeTransform>
                            </LinearGradientBrush>
                        </TextBlock.Foreground>

                        <TextBlock.Effect>
                            <DropShadowEffect Color="{DynamicResource PrimaryColor}"
                                              BlurRadius="10"
                                              ShadowDepth="0"
                                              Opacity="0.8"/>
                        </TextBlock.Effect>

                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation
                                            Storyboard.TargetName="GradientTranslate"
                                            Storyboard.TargetProperty="X"
                                            From="0"
                                            To="1"
                                            Duration="0:0:1.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>

                    <!-- Window Control Button -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,0">
                        <Button Style="{StaticResource CloseButton}" Click="CloseButton_Click">
                            <TextBlock Text="🗙" FontSize="16" VerticalAlignment="Center" Margin="0,-3,0,0"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Settings Content -->
            <Grid Grid.Row="1" Margin="30">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- App Behavior Section -->
                        <TextBlock Text="App Behavior" FontSize="18" FontWeight="Bold" Margin="0,0,0,15">
                            <TextBlock.Foreground>
                                <LinearGradientBrush x:Name="AppBehaviorGradient"
                             StartPoint="0,0"
                             EndPoint="1,0"
                             SpreadMethod="Repeat">
                                    <GradientStop Color="{DynamicResource SecondaryColor}" Offset="0.3"/>
                                    <GradientStop Color="{DynamicResource PrimaryColor}" Offset="1.0"/>
                                    <GradientStop Color="{DynamicResource SecondaryColor}" Offset="0.3"/>

                                    <LinearGradientBrush.RelativeTransform>
                                        <TransformGroup>
                                            <TranslateTransform x:Name="AppBehaviorGradientTranslate" X="0"/>
                                            <RotateTransform Angle="-40" CenterX="0.5" CenterY="0.5"/>
                                        </TransformGroup>
                                    </LinearGradientBrush.RelativeTransform>
                                </LinearGradientBrush>
                            </TextBlock.Foreground>

                            <TextBlock.Effect>
                                <DropShadowEffect Color="{DynamicResource PrimaryColor}"
                          BlurRadius="10"
                          ShadowDepth="0"
                          Opacity="0.8"/>
                            </TextBlock.Effect>

                            <TextBlock.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation
                        Storyboard.TargetName="AppBehaviorGradientTranslate"
                        Storyboard.TargetProperty="X"
                        From="0"
                        To="1"
                        Duration="0:0:1.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </TextBlock.Triggers>
                        </TextBlock>
                        <CheckBox x:Name="LaunchOnStartupCheckBox" 
                                  Content="Launch on Windows startup"/>
                        <CheckBox x:Name="MinimizeToTrayCheckBox" 
                                  Content="Start minimized to system tray"/>
                        <CheckBox x:Name="ShowVersionCheckBox"
                                  Content="Show version in title bar"
                                  Margin="0,0,0,10"/>
                        <CheckBox x:Name="ShowToastCheckBox"
                                  Content="Enable toast notifications" />


                        <!-- Security Section -->
                        <TextBlock Text="Security" FontSize="18" FontWeight="Bold" Margin="0,20,0,15">
                            <TextBlock.Foreground>
                                <LinearGradientBrush x:Name="SecurityGradient"
                             StartPoint="0,0"
                             EndPoint="1,0"
                             SpreadMethod="Repeat">
                                    <GradientStop Color="{DynamicResource SecondaryColor}" Offset="0.3"/>
                                    <GradientStop Color="{DynamicResource PrimaryColor}" Offset="1.0"/>
                                    <GradientStop Color="{DynamicResource SecondaryColor}" Offset="0.3"/>

                                    <LinearGradientBrush.RelativeTransform>
                                        <TransformGroup>
                                            <TranslateTransform x:Name="SecurityGradientTranslate" X="0"/>
                                            <RotateTransform Angle="-40" CenterX="0.5" CenterY="0.5"/>
                                        </TransformGroup>
                                    </LinearGradientBrush.RelativeTransform>
                                </LinearGradientBrush>
                            </TextBlock.Foreground>

                            <TextBlock.Effect>
                                <DropShadowEffect Color="{DynamicResource PrimaryColor}"
                          BlurRadius="10"
                          ShadowDepth="0"
                          Opacity="0.8"/>
                            </TextBlock.Effect>

                            <TextBlock.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation
                        Storyboard.TargetName="SecurityGradientTranslate"
                        Storyboard.TargetProperty="X"
                        From="0"
                        To="1"
                        Duration="0:0:1.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </TextBlock.Triggers>
                        </TextBlock>
                        <CheckBox x:Name="ClipboardAutoClearCheckBox" 
                                  Content="Auto-clear clipboard after copying password"
                                  Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal" Margin="20,0,0,20">
                            <TextBlock Text="Timeout:" 
                                       Foreground="{DynamicResource SubTextBrush}" 
                                       FontWeight="Normal"
                                       VerticalAlignment="Center" 
                                       Margin="0,0,10,0"/>
                            <ComboBox x:Name="ClipboardTimeoutComboBox" Width="120">
                                <ComboBoxItem Content="10 seconds" Tag="10"/>
                                <ComboBoxItem Content="30 seconds" Tag="30"/>
                                <ComboBoxItem Content="60 seconds" Tag="60" IsSelected="True"/>
                            </ComboBox>
                        </StackPanel>

                        
                        <!-- Save Button -->
                        <Button Content="💾 Save Settings" 
                                Click="SaveSettings_Click" 
                                HorizontalAlignment="Right"
                                Margin="0,20,0,0"/>

                        

                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Border>
</Window>